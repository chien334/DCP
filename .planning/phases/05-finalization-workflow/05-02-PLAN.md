---
phase: 05-finalization-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/ProcureFlow.Infrastructure/Data/Configurations/FinalizeConfiguration.cs
  - src/ProcureFlow.Infrastructure/Data/ApplicationDbContext.Finalize.cs
  - src/ProcureFlow.Web/Endpoints/Buyer/RfpFinalizeEndpoints.cs
  - tests/ProcureFlow.Web.IntegrationTests/Buyer/RfpFinalizeEndpointsTests.cs
autonomous: true
requirements:
  - FIN-02
---

<objective>
Persist immutable finalization pricing snapshot at line-item level.

Purpose: Lock commercial values at award time so subsequent bid edits cannot mutate finalized totals.
Output: Finalize item snapshot mapping and retrieval endpoint behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate and persist finalize item snapshots</name>
  <files>src/ProcureFlow.Web/Endpoints/Buyer/RfpFinalizeEndpoints.cs, src/ProcureFlow.Infrastructure/Data/Configurations/FinalizeConfiguration.cs</files>
  <action>During finalization, copy bid item values into finalize item snapshots and expose GET /api/buyer/rfps/{rfpId}/finalize to return snapshot values independent from mutable bid items.</action>
  <verify>
    <automated>dotnet test tests/ProcureFlow.Web.IntegrationTests --filter "FIN-02"</automated>
  </verify>
</task>

</tasks>

<success_criteria>
FIN-02 passes with immutable snapshot behavior validated after bid mutation attempts.
</success_criteria>
