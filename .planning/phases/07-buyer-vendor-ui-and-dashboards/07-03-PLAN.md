---
phase: 07-buyer-vendor-ui-and-dashboards
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/ProcureFlow.Web/Components/Pages/Vendor/VendorDashboard.razor
  - src/ProcureFlow.Web/Components/Pages/Vendor/RfpInviteListPage.razor
  - src/ProcureFlow.Web/Components/Pages/Vendor/BidCreatePage.razor
  - src/ProcureFlow.Web/Components/Pages/Vendor/BidEditPage.razor
  - src/ProcureFlow.Web/Components/Pages/Vendor/ContractSignPage.razor
autonomous: true
requirements:
  - UI-06
  - UI-07
---

<objective>
Build vendor dashboard and workflow pages for invite acceptance, bid submission/editing, and contract signing.

Purpose: Enable vendors to respond to RFP invites, submit and manage bids, and sign contracts through streamlined UI.
Output: Dashboard with pending invites and bid status, bid creation/edit forms with spec input, and contract signing page with decline option.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement vendor dashboard and invite list</name>
  <files>src/ProcureFlow.Web/Components/Pages/Vendor/VendorDashboard.razor, src/ProcureFlow.Web/Components/Pages/Vendor/RfpInviteListPage.razor</files>
  <action>Create vendor dashboard showing pending invites (count, deadlines), active bids, and signed contracts. Build invite list page with accept/decline buttons, RFP preview, and deadline countdown. Include confirmation dialogs.</action>
  <verify>
    <manual>Dashboard loads vendor's pending invites; invite list displays with accept/decline buttons functional; decisions post to backend</manual>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement bid submission and editing forms</name>
  <files>src/ProcureFlow.Web/Components/Pages/Vendor/BidCreatePage.razor, src/ProcureFlow.Web/Components/Pages/Vendor/BidEditPage.razor</files>
  <action>Build bid creation form with RFP item/spec display and vendor input fields (quantity, unit price, notes). Implement bid item spec inputs (text/number/boolean types). Add totals calculation and preview. Create edit form for bid updates before submission deadline.</action>
  <verify>
    <manual>Bid form displays RFP specs; vendor inputs calculate totals; form posts and shows success/error; edit loads existing bid data</manual>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement contract signing and decline workflow</name>
  <files>src/ProcureFlow.Web/Components/Pages/Vendor/ContractSignPage.razor</files>
  <action>Build contract sign page showing contract details, finalized pricing, and signature fields. Implement sign button with confirmation modal. Add decline button with note textarea for rejection reason. Show successful signing confirmation.</action>
  <verify>
    <manual>Contract page displays all contract data; sign button triggers confirmation and posts signature; decline includes note; both update UI with success message</manual>
  </verify>
</task>

</tasks>

<success_criteria>
UI-06, UI-07: Vendor workflows are complete from invite acceptance through bid submission to contract signing/decline; all forms post correctly and reflect server state.
</success_criteria>
